# Extraction Workspace

Use this folder for extraction scripts, intermediate tables, OCR notes, QA logs, and generated diagnostics.

The project root should stay limited to:

- `index.html`
- `moose_train_schedule.csv`
- `supporting_files/`
